# This demo shows how to install minikube and play kubernetes on your Mac.

# install kubectl
brew install kubectl

# install minikube
brew install minikube

# start minikube
minikube start

# show the cluster information
kubect cluster-info
kubectl get nodes

# create a pod
kubectl run test-pod --image=nginx --wait
kubectl get pods  

# ssh to the pod container
kubectl exec test-pod -i -- /bin/sh

# delete the pod
kubectl delete pod test-pod

# shutdown minikube
minikube stop
minikube delete

# That's it. 
# Thank you!
